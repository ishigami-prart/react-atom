import styled from "styled-components";

export const Top = () => {
  return (
    <SContainer>
      <h2>ユーザー一覧</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
